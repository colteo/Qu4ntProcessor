class ParametersModel:

    def __init__(self, processor_type, data_feed, strategy, broker):
        self.processor_type = processor_type
        self.data_feed = data_feed
        self.strategy = strategy
        self.broker = broker
